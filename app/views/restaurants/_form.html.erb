<%= form_with model: @restaurant, local: true do |form| %>

<% if @restaurant.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@restaurant.errors.count, "error") %>
      prohibited this restaurant from being saved:
    </h2>
    <ul>
      <% @resturant.errors.full_messages do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<p><%= form.label :name %></p></br>
<p><%= form.text_field :name %>

<p><%= form.label :description %></p><br>
<p><%= form.text_area :description %>

  <p>
    <%= form.submit %>
  </p>

  <% end %>
